baseURL = "https://edu2610.github.io/"
title = "Eduardo Vergara — Portafolio"
languageCode = "es"
theme = "PaperMod"
canonifyURLs = true

[params]
  defaultTheme = "auto"

[params.assets]
  # Evita rutas fingerprint que a veces no resuelven bien en Pages
  disableFingerprinting = true
